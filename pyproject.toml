[project]
name = "otto-romi-ng"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31.0",
    "dotenv>=0.9.9",
    "minify-html>=0.18.1",
    "quart>=0.22.0",
]

[dependency-groups]
dev = [
    "ty>=0.0.74",
]

[tool.ty.environment]
python = ".venv"
