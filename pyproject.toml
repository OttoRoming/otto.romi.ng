[project]
name = "otto-romi-ng"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "quart>=0.22.0",
]

[dependency-groups]
dev = [
    "ty>=0.0.74",
]
